<Window x:Class="TimeTrackingApp.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        UseLayoutRounding="True" 
             SnapsToDevicePixels="True">

    <Border Background="White" CornerRadius="10" Padding="20" BorderBrush="Gray" BorderThickness="1">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="StatusText" Text="Обновление графика..." FontSize="16" FontWeight="Bold" Margin="10" HorizontalAlignment="Center"/>
            <ProgressBar x:Name="ProgressBar" Width="200" Height="20" Margin="10" Minimum="0" Maximum="100" />
        </StackPanel>
    </Border>
</Window>